extern volatile int buttonon;               // Button State
extern volatile int portinprev;    // Previous Clock cycle's port in
extern volatile int count;                  // Timer Count
extern volatile int swapflag;               // Set to 1 after Button swapped places
extern volatile int len;
extern volatile int hex;
extern volatile char letter;

